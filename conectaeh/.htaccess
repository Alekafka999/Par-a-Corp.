Options -Indexes

<IfModule mod_authz_core.c>
  <FilesMatch "\.pdf$">
    Require all denied
  </FilesMatch>
</IfModule>

<IfModule !mod_authz_core.c>
  <FilesMatch "\.pdf$">
    Order allow,deny
    Deny from all
  </FilesMatch>
</IfModule>
